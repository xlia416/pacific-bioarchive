# Serverless Devs 安装到 FC 运行时的依赖（FC Python 运行时用 s.yaml 的 artifacts 上传）
# FC Python 3.10 运行时预装 pyOpenSSL 22.1.0，其 ABI 要求
# cryptography<39。锁定整组网络/加密依赖，避免构建机拉取
# 新版后在 FC 内出现 `module 'lib' has no attribute 'GEN_EMAIL'`。
PyJWT==2.8.0
requests==2.31.0
cryptography==38.0.4
oss2==2.19.1
urllib3==1.26.20
charset-normalizer==2.1.1
jmespath==0.10.0
